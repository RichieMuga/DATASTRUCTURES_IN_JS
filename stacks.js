// stacks are a lifo datastructure(last in first out)
// imagine a stack of plates whereby one can add to the top and only remove the top plates
// examples where stacks are used:
// --stack overflow
// --when browsing through pages, when you go back it accesses the page before the current page. ie. stacks
// --when using undo and redo in your code editor or word document or etc.

